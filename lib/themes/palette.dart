import 'package:flutter/material.dart';

// Soft primary (muted green)
const primaryColor = Color(0xFF7FAF9B);

// Warm pale background (not pure white, easier on eyes)
const backgroundColor = Color(0xFFF5F7F4);

// Main text (soft black, not harsh)
const mainTextColor = Color(0xFF2F3A34);

// Icons & blocks (slightly deeper green for contrast)
const iconsBlocksColor = Color(0xFF5E8C7A);

// Labels (subtle green-gray)
const labelsColor = Color(0xFF6F8F80); // author, song, etc.

// Accent (very soft earthy tone instead of strong orange)
const auxiliarColor = Color(0xFFD8A48F);

// True white (for cards if needed)
const cardColor = Color(0xFFFFFFFF);
const cardColorSoft = Color(0xFFF0F4F1);
